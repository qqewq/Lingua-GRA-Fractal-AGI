# Fractal Dimension Analysis – README

> 🇷🇺 Пример воспроизводимого эксперимента по оценке фрактальной размерности эмбеддингов.  
> 🇬🇧 Example of a reproducible experiment for estimating the fractal dimension of embeddings.

---

## 🇷🇺 1. Описание

Этот пример показывает, как:

- обучить (или загрузить) word2vec-эмбеддинги для русского корпуса;
- выбрать подмножество слов и извлечь их векторные представления;
- оценить **корреляционную размерность D₂** этого множества точек в \(\mathbb{R}^d\) с помощью алгоритма типа Grassberger–Procaccia;[web:38][web:40][web:160]  
- использовать полученное значение D₂ как эталонное \(D_{\text{target}}\) для фрактального регуляризатора Lingua GRA.

---

## 🇷ГУ 2. Требования

- Python 3.10+
- PyTorch
- gensim (для загрузки word2vec-модели)
- numpy
- Этот репозиторий установлен как пакет (`pip install -e .`).

---

## 🇷ГУ 3. Подготовка эмбеддингов

1. Соберите текстовый корпус (например, научные аннотации или смешанный корпус).  
2. Обучите word2vec (можно с помощью gensim, см. документацию и примеры).[web:202][web:204]  
3. Сохраните модель в формате word2vec (`.txt` или бинарный).

Пример тренировки word2vec (gensim):

```python
from gensim.models import Word2Vec
from gensim.utils import simple_preprocess

sentences = [simple_preprocess(line) for line in open("corpus_all.txt", encoding="utf-8")]
model = Word2Vec(
    sentences,
    vector_size=100,
    window=5,
    min_count=5,
    workers=4,
)
model.wv.save_word2vec_format("embeddings.vec", binary=False)
