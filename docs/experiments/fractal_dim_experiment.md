# Fractal Dimension Experiment – Эксперимент с фрактальной размерностью

> 🇷🇺 Ниже — протокол эксперимента: сказки vs научные тексты, оценка фрактальной размерности эмбеддингов и использование её как целевого параметра для Lingua GRA.  
> 🇬🇧 Below is an experiment protocol: fairy tales vs scientific texts, estimation of fractal dimension of embeddings and using it as a target parameter for Lingua GRA.

---

## 🇷🇺 1. Цель эксперимента

Проверить две гипотезы:

1. **Разные жанры языка (сказки vs наука) имеют разные фрактальные профили** в embedding-пространстве, аналогично тому, как разные естественные языки образуют отличающиеся мультифрактальные структуры.[web:38][web:34]  
2. **Фрактальный регуляризатор Lingua GRA может подтянуть язык AGI** ближе к целевому жанру (научному), если использовать его фрактальные параметры как \(D_{\text{target}}\).

---

## 🇷🇺 2. Данные

### 2.1. Корпус A: сказки

- Небольшой корпус детских/художественных сказок на русском.
- 50–200 текстов, длиной 1–5 страниц каждый.
- Минимальная предварительная очистка:
  - убрать HTML/разметку;
  - привести к UTF-8;
  - разбить на предложения.

### 2.2. Корпус B: научные аннотации

- Корпус научных аннотаций/абстрактов (например, переводы/оригиналы arXiv или российских журналов).
- 1–5 тыс. абстрактов (или меньше, если нет доступа к большому корпусу).
- Очистка:
  - удалить LaTeX-формулы (оставить только текст);
  - удалить слишком короткие фрагменты.

---

## 🇷🇺 3. Эмбеддинги

### 3.1. Слово/предложение

Два варианта:

1. **word2vec–эмбеддинги слов** для воспроизводимости идей Ribeiro et al.:[web:38][web:34]
   - обучаем отдельную модель word2vec (gensim) на объединённом корпусе A+B;
   - для каждого корпуса берём:
     - либо топ-N слов по частоте;
     - либо все слова выше порога частоты.

2. **Эмбеддинги предложений** (семантический уровень Lingua GRA):
   - используем собственный encoder (Transformer) или внешний sentence encoder;
   - для каждого предложения обоих корпусов считаем эмбеддинг.

В этом эксперименте разумно сделать оба варианта, но **минимальный прототип** — вариант (1) с word2vec.

### 3.2. Обучение word2vec

Пример с gensim:

```python
from gensim.models import Word2Vec
from gensim.utils import simple_preprocess

sentences = [simple_preprocess(line) for line in open("corpus_all.txt", encoding="utf-8")]
model = Word2Vec(
    sentences,
    vector_size=100,
    window=5,
    min_count=5,
    workers=4,
)
