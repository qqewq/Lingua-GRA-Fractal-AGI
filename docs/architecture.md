# Архитектура Lingua GRA / Lingua GRA Architecture

> 🇷🇺 Ниже — описание архитектуры уровней языка, GRA-ядра и фрактального слоя.  
> 🇬🇧 Below is a description of the language levels, GRA core and fractal layer architecture.

---

## 🇷🇺 1. Общий обзор

Архитектура Lingua GRA строится вокруг трёх слоёв:

1. **GRA-ядро** — абстракции гильбертовых пространств, проекторов и функционала «пены».
2. **Языковые уровни** — конкретизация этих абстракций для символьного, синтаксического, семантического, прагматического и мета-уровней языка.
3. **Нейросетевые реализации** — энкодеры/декодеры и проекторы, аппроксимирующие GRA-операторы, плюс фрактальный слой.

Кодовая организация:

- `src/gra_core.py` — ядро GRA.
- `src/language_levels.py` — описание уровней языка.
- `src/fractal_utils.py` — фрактальные метрики и регуляризатор.
- `src/neural_encoders.py`, `src/training.py` — нейросетевые модули и обучение.

---

## 🇷🇺 2. GRA-ядро (`gra_core.py`)

### 2.1. HilbertSpace

Абстракция пространства \(\mathcal{H}\):

- размерность (конечная или эффективная);
- тип носителя (вектор, дерево, граф, последовательность);
- базовые операции:
  - инициализация векторов;
  - скалярное произведение;
  - норма.

В реализации:

- это может быть thin-обёртка над `torch.Tensor` с метаданными;
- для нестандартных объектов (деревья/графы) — эффективное представление в \(\mathbb{R}^d\).

### 2.2. Projector

Проектор \(\mathcal{P}_G : \mathcal{H} \to \mathcal{H}\) задаёт цель уровня:

- аналитические проекторы — для простых подпространств (например, ограничение по маске);
- нейросетевые проекторы \(P_\theta\) — обучаемое приближение \(\mathcal{P}_G\);
- итеративные проекторы — фиксированные итерации к точке минимума (fixed point).

Интерфейс:

- `forward(x)` → \( \mathcal{P}_G x \),
- `foam(x)` → \(\|(1 - \mathcal{P}_G)x\|^2\).

### 2.3. GRAFunctional

Инкапсулирует вычисление GRA-функционала:

- хранит набор проекторов \(\{\mathcal{P}_{G_l}\}\) и весов \(\{\Lambda_l\}\);
- считает:

  ```python
  J = sum(Lambda[l] * projector_l.foam(psi_l) for l in levels)
