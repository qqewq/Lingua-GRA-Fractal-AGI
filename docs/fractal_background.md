# Fractal Background – Фрактальный фон языка и эмбеддингов

> 🇷🇺 Ниже — обзор работ о фрактальной природе языка и о том, как это используется в Lingua GRA.  
> 🇬🇧 Below is an overview of fractal properties of language and how they motivate the Lingua GRA fractal layer.

---

## 🇷🇺 1. Зачем нам фракталы в языке

Идея Lingua GRA: язык AGI не просто «набор эмбеддингов», а фрактальный объект в высокомерном пространстве.  
Фрактальные характеристики (размерности, спектры) отражают:

- насколько «распластано» или «собрано» языковое пространство;
- как связаны близкие и далёкие по смыслу слова/конструкции;
- отличаются ли стили (научный vs художественный) геометрией эмбеддингов.

Цель фрактального слоя:

> Сделать фрактальные параметры **явной целью** при обучении языка AGI, а не побочным эффектом.

---

## 🇷🇺 2. Фрактальные структуры в word2vec–пространствах

### 2.1. Мультифрактальные языки

Работа Ribeiro et al. «On the fractal patterns of language structures» (PLOS ONE, 2023) анализирует word2vec-эмбеддинги 18 языков (включая русский) и показывает, что:

- для каждого языка строится 100-мерное embedding-пространство слов;[web:38]  
- для облаков точек вычисляются фрактальные размерности с помощью box-counting на разных масштабах;[web:38][web:34]  
- полученные структуры оказываются **мультифрактальными**: есть как минимум две характерные размерности — для малых и больших масштабов расстояний.

Если взять:

- длинномасштабную фрактальную размерность;
- отношение type/token (разнообразие слов),

то языки в этом 2D-пространстве кластеризуются в группы, соответствующие их филогенетическим корням (индоевропейские, финно-угорские и т.д.).[web:38][web:34][web:97]

**Вывод для Lingua GRA**:

- фрактальная размерность эмбеддингов отражает **глубинную структурную близость языков**;
- фрактальные параметры можно использовать как маркеры стиля/типа языка.

### 2.2. Интерпретация фрактальной размерности

Ribeiro et al. обсуждают, что:

- фрактальная размерность на **коротких масштабах** связана с локальными структурами — словами со схожим употреблением и значением;
- фрактальная размерность на **длинных масштабах** связана с глобальной организацией лексики — более различными по смыслу участками пространства.[web:34]

Это означает: изменение фрактальной размерности при переходе от художественного к научному корпусу будет отражать:

- степень тематической «собранности»;
- плотность терминологических кластеров;
- разнообразие связей между темами.

---

## 🇷🇺 3. Intrinsic dimensions языковых фрактальных структур

### 3.1. Language fractal structures

Статья «A Language and Its Dimensions: Intrinsic Dimensions of Language Fractal Structures» вводит объект:

- **language fractal structure** — множество эмбеддингов всех n-грамм языка (Hailonakea как сумма всех таких структур).[web:45]  

Авторы:

- для русского и английского языка строят эмбеддинги n-грамм;
- оценивают **intrinsic dimensions** (подлинные, «фрактальные» размерности) двумя методами:
  - топологический анализ (topological data analysis);
  - анализ минимального остовного дерева (MST) и теорема Steele.[web:45][web:102]

Результат:

- для обоих языков и для многих n intrinsic dimensions:
  - **нецелые** (типично для фракталов);
  - находятся около значения ~9.[web:45][web:35][web:108]

Это интерпретируется как:

- указание на существование устойчивого фрактального инварианта языка;
- возможность рассматривать язык как фрактальный объект с характерной размерностью.

### 3.2. Связь с Lingua GRA

Для Lingua GRA это даёт:

- мотивацию задавать **целевые фрактальные размерности** \(D^{(l)}_{\text{target}}\) для семантического уровня, близкие к наблюдаемым значениям для развитых языков;
- возможность сравнивать «языки AGI» с человеческими по intrinsic dimensions и оценивать их «зрелость».

---

## 🇷🇺 4. Корреляционная размерность и LLM

### 4.1. Grassberger–Procaccia в статистическом многообразии

Работа «Correlation Dimension of Natural Language in a Statistical Manifold» (2024) измеряет корреляционную размерность языка, применяя алгоритм Grassberger–Procaccia к:

- последовательностям, порождённым крупной языковой моделью;
- но не в евклидовом пространстве, а в **статистическом многообразии** с расстоянием Fisher–Rao.[web:105][web:41][web:44]

Ключевые результаты:

- язык демонстрирует **мультифрактальность** и глобальное самоподобие;
- для разных языков (английский, китайский, японский, немецкий) наблюдается **универсальная глобальная размерность** около \(\hat{\nu} \approx 6.5\);[web:105]  
- эта размерность:
  - меньше, чем у белого шума в том же многообразии (>10);
  - больше, чем у процессов Барбаши–Альберт (около 2–3.5).[web:105]

### 4.2. Интерпретация

Это показывает, что:

- фрактальные свойства языка — не артефакт конкретного эмбеддинга (word2vec), а фундаментальная характеристика;
- корреляционная размерность D₂ — пригодная для практики величина, которую можно:
  - оценивать на мини-батчах;
  - использовать как цель в регуляризаторе.

Для Lingua GRA это напрямую поддерживает идею фрактального регуляризатора \(R_{\text{fract}}\), основанного на D₂.

---

## 🇷🇺 5. Как фракталы встроены в Lingua GRA

### 5.1. Фрактальный регуляризатор

На уровне \(l\) рассматриваем эмбеддинги \(X^{(l)} = \{x_i\} \subset \mathbb{R}^d\). Определяем:

- оценку корреляционной размерности \(D^{(l)}_{\text{current}}\) (GP или box-counting);[web:38][web:40][web:42]  
- целевое значение \(D^{(l)}_{\text{target}}\) (например, взятое из научных текстов или «здорового» языка).

Тогда:

\[
R_{\text{fract}}(\Psi^{(l)}) =
\left(D^{(l)}_{\text{current}} - D^{(l)}_{\text{target}}\right)^2.
\]

Функционал уровня:

\[
\tilde{\Phi}^{(l)}(\Psi^{(l)}, G_l) =
\|(1 - \mathcal{P}_{G_l}) \Psi^{(l)}\|^2
+ \gamma_l R_{\text{fract}}(\Psi^{(l)}).
\]

Полный функционал:

\[
J_{\text{fractal}}(\Psi) =
\sum_{l=0}^{L} \Lambda_l \tilde{\Phi}^{(l)}(\Psi^{(l)}, G_l).
\]

### 5.2. Жанровые и стилевые профили

Поскольку фрактальные параметры чувствительны к структуре и типу языка,[web:38][web:45]

- художественные тексты, научные статьи, технические спецификации будут иметь разные фрактальные профили;
- Lingua GRA может:

  - измерять профиль (D₂, intrinsic dimensions) корпуса;
  - использовать его как эталон для AGI-языка в конкретной задаче.

В `docs/experiments/fractal_dim_experiment.md` описан пример:

- сравнение фрактальной размерности эмбеддингов детских сказок и научных аннотаций;
- настройка регуляризатора, чтобы AGI-язык приближался к научному профилю.

---

## 🇷🇺 6. Мультифрактальность и будущее развитие

Факт мультифрактальности (несколько размерностей на разных масштабах)[web:38][web:105] подсказывает расширения:

1. **Мультифрактальный спектр**  
   Вместо одного числа D₂ — набор D_q или спектр \(f(\alpha)\):

   - измерять несколько моментов и их зависимость от масштаба;
   - вводить регуляризатор, приближающий спектр к эталонному.

2. **Масштаб-зависимая цель**  
   Можно задавать разные целевые размерности:

   - для малых r (локальная структура);
   - для больших r (глобальная структура).

3. **Связь с когнитивными качествами AGI**  
   Гипотеза: фрактальный профиль языка коррелирует с:

   - глубиной аргументации;
   - «научностью» мышления;
   - устойчивостью к противоречиям и галлюцинациям.

Исследование этих связей — одна из целей проекта.

---

## 🇬🇧 7. English summary (short)

### 7.1. Fractals in language embeddings

- Word2vec embeddings of many languages form **multifractal structures** with distinct fractal dimensions at short and long scales.[web:38][web:34][web:97]  
- Intrinsic dimensions of language fractal structures (embeddings of all n-grams) are non-integer and close to 9 for Russian and English; this supports a fractal view of language as a geometric object.[web:45][web:35][web:108]  
- The correlation dimension of natural language in a statistical manifold, estimated by a Grassberger–Procaccia-type algorithm, yields a universal value around 6.5 across several languages.[web:105][web:41][web:44]

### 7.2. Role in Lingua GRA

Lingua GRA uses these results to define a **fractal regularizer**:

\[
R_{\text{fract}}(\Psi^{(l)}) =
\Big(D^{(l)}_{\text{current}} - D^{(l)}_{\text{target}}\Big)^2,
\]

where \(D^{(l)}_{\text{current}}\) is a batch-based estimate of fractal (e.g. correlation) dimension, and \(D^{(l)}_{\text{target}}\) is derived from reference corpora (e.g. scientific texts). This term is added to each level’s foam, making the AGI language both:

- internally consistent (GRA annihilation),
- geometrically aligned with desired fractal profiles.

Different genres and styles correspond to different fractal signatures, so the AGI language can be steered towards “scientific”, “technical” or other cognitive regimes by adjusting the target fractal parameters.[web:38][web:45][web:105]
